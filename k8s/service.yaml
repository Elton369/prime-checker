apiVersion: v1
kind: Service
metadata:
  name: prime-checker-service
  namespace: prime-checker
  labels:
    app: prime-checker
spec:
  selector:
    app: prime-checker
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP